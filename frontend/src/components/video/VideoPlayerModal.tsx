"use client";
import { AnimatePresence, motion } from 'framer-motion';
import { useEffect } from 'react';
import type { YouTubeVideo } from '@/lib/youtube';
export default function VideoPlayerModal({video,onClose}:{video:YouTubeVideo|null;onClose:()=>void}){useEffect(()=>{if(!video)return;const close=(event:KeyboardEvent)=>{if(event.key==='Escape')onClose()};document.body.style.overflow='hidden';window.addEventListener('keydown',close);return()=>{document.body.style.overflow='';window.removeEventListener('keydown',close)}},[video,onClose]);return <AnimatePresence>{video&&<div className="fixed inset-0 z-[100] grid place-items-center p-3 sm:p-6"><motion.button aria-label="Close video" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="absolute inset-0 bg-[#03190d]/90 backdrop-blur-xl" onClick={onClose}/><motion.section role="dialog" aria-modal="true" aria-labelledby="video-player-title" initial={{opacity:0,y:20,scale:.98}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:15}} className="relative z-10 w-full max-w-5xl overflow-hidden rounded-2xl bg-[#061b10] shadow-2xl sm:rounded-3xl"><div className="relative aspect-video"><iframe className="absolute inset-0 h-full w-full" src={`${video.embedUrl}?autoplay=1&rel=0`} title={video.title} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen/></div><div className="flex items-start justify-between gap-5 p-5 text-white sm:p-6"><div><p className="mb-1 text-[10px] font-extrabold uppercase tracking-[.18em] text-isoko-gold">{video.channelTitle}</p><h2 id="video-player-title" className="text-base font-bold leading-6 sm:text-xl">{video.title}</h2></div><button onClick={onClose} className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-white/10 text-xl" aria-label="Close">×</button></div></motion.section></div>}</AnimatePresence>}
