"use client";
import Image from 'next/image';
import { motion } from 'framer-motion';
import type { YouTubeVideo } from '@/lib/youtube';
import { formatCount, formatDuration } from '@/lib/youtube';
export default function YouTubeVideoCard({video,index=0,onPlay,locale='en'}:{video:YouTubeVideo;index?:number;onPlay:(video:YouTubeVideo)=>void;locale?:string}){return <motion.article initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-50px'}} transition={{delay:Math.min(index*.05,.2)}} className="group overflow-hidden rounded-2xl border border-isoko-dark/8 bg-white shadow-[0_10px_35px_rgba(6,59,31,.06)] transition hover:-translate-y-1 hover:shadow-[0_20px_55px_rgba(6,59,31,.12)]"><button onClick={()=>onPlay(video)} className="relative block aspect-video w-full overflow-hidden bg-isoko-dark text-left" aria-label={`Play ${video.title}`}><Image fill sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw" src={video.thumbnail} alt={video.title} className="object-cover transition duration-500 group-hover:scale-105"/><span className="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent"/><span className="absolute left-4 top-4 grid h-12 w-12 place-items-center rounded-full bg-red-600 text-white shadow-xl transition group-hover:scale-110"><i className="fa-solid fa-play ml-0.5"/></span>{formatDuration(video.duration)&&<span className="absolute bottom-3 right-3 rounded-md bg-black/80 px-2 py-1 text-[10px] font-extrabold text-white">{formatDuration(video.duration)}</span>}</button><div className="p-5"><p className="mb-2 text-[10px] font-extrabold uppercase tracking-[.14em] text-isoko-accent">{video.channelTitle}</p><h3 className="line-clamp-2 text-[15px] font-bold leading-6 text-isoko-dark">{video.title}</h3><p className="mt-2 line-clamp-2 text-xs leading-5 text-slate-500">{video.description}</p><div className="mt-4 flex flex-wrap items-center gap-x-4 gap-y-2 border-t border-isoko-dark/6 pt-4 text-[11px] font-bold text-slate-500"><span><i className="fa-regular fa-eye mr-1.5 text-isoko-accent"/>{formatCount(video.viewCount,locale)}</span><span><i className="fa-regular fa-thumbs-up mr-1.5 text-isoko-accent"/>{formatCount(video.likeCount,locale)}</span><span><i className="fa-regular fa-comment mr-1.5 text-isoko-accent"/>{formatCount(video.commentCount,locale)}</span><time className="ml-auto" dateTime={video.publishedAt}>{new Date(video.publishedAt).toLocaleDateString(locale,{year:'numeric',month:'short',day:'numeric'})}</time></div></div></motion.article>}
