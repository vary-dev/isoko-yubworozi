import type { Metadata } from "next";
export const metadata: Metadata = { title: "Privacy Policy", description: "How Isoko y'Ubworozi handles account, resource-access and payment-verification information.", alternates: { canonical: "/privacy" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
