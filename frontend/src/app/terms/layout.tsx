import type { Metadata } from "next";
export const metadata: Metadata = { title: "Terms of Use", description: "Terms for using Isoko y'Ubworozi accounts, lessons, free books and premium resources.", alternates: { canonical: "/terms" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
